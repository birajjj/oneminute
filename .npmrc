# @anthropic-ai/sdk lists zod ^3.25 as an OPTIONAL peer; this project pins
# zod 3.24 and doesn't use the SDK's zod helpers. Allow the optional-peer
# mismatch so installs succeed here and on Vercel without --force.
legacy-peer-deps=true
